[alias]
test = "test -- --test-threads=1"
t = "test -- --test-threads=1"

[build]
# Default target for consistent builds
# target = "x86_64-unknown-linux-gnu"

[env]
# Set any environment variables needed for testing
# RUST_LOG = "debug"
